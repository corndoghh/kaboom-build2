{
  "name": "kaboom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd src/scripts/game/ && esbuild game.js --bundle --format=esm > build.js && cd .. && cd .. && cd .. && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.17",
    "express": "^4.18.2",
    "kaboom": "^2000.2.10"
  }
}
